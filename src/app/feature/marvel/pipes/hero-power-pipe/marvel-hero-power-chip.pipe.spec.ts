import { MarvelHeroPowerChipPipe } from './marvel-hero-power-chip.pipe';

describe('MarvelHeroPowerChipPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelHeroPowerChipPipe();
    expect(pipe).toBeTruthy();
  });
});
