import { HeroGenderPipe } from './hero-gender.pipe';

describe('HeroGenderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
