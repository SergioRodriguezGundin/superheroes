import { MarvelHeroMemberOfPipe } from './marvel-hero-member-of.pipe';

describe('MarvelHeroMemberOfPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelHeroMemberOfPipe();
    expect(pipe).toBeTruthy();
  });
});
